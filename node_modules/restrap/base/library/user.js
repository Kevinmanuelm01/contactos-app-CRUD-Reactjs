var user = function(self){
  this.orm = self.library('orm');
  return this;
};

user.prototype.read = function(id){
  var d = q.defer();
  this.orm.user.find({ where: { id: id }}).then(d.resolve, d.reject);
  return d.promise;
};

module.exports = function(self){
  return new user(self);
};
